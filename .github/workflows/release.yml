name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build-and-release:
    runs-on: macos-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Select Xcode
        run: |
          xcode_path=$(ls -d /Applications/Xcode*.app | sort -V | tail -1)
          sudo xcode-select -s "$xcode_path"

      - name: Build Release
        run: |
          xcodebuild build \
            -project MMCL.xcodeproj \
            -scheme MMCL \
            -destination 'platform=macOS' \
            -configuration Release \
            -derivedDataPath build \
            MARKETING_VERSION="${GITHUB_REF_NAME#v}" \
            CURRENT_PROJECT_VERSION=2 \
            ARCHS="arm64 x86_64" \
            ONLY_ACTIVE_ARCH=NO \
            CODE_SIGNING_ALLOWED=NO \
            MACOSX_DEPLOYMENT_TARGET=14.0 \
            -quiet

      - name: Create ZIP and DMG
        run: |
          app_path="build/Build/Products/Release/MMCL.app"
          echo "Found app at: $app_path"
          ditto -c -k --sequesterRsrc --keepParent "$app_path" \
            "MMCL-${GITHUB_REF_NAME}.zip"
          hdiutil create -volname "MMCL" \
            -srcfolder "$app_path" \
            -ov -format UDZO \
            "MMCL-${GITHUB_REF_NAME}.dmg"

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            MMCL-${{ github.ref_name }}.zip
            MMCL-${{ github.ref_name }}.dmg
          generate_release_notes: true
